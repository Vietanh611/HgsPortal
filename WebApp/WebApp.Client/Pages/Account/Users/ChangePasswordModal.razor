@using Hgs.Share.Requests.Users
@using BlazorBootstrap

<Modal @ref="modal" Title="@($"Đổi mật khẩu - {Username}")" OnClose="Close" Size="ModalSize.Large">
    <BodyTemplate>
        <EditForm Model="@ChangePasswordForm" OnValidSubmit="HandleSubmit">
            <DataAnnotationsValidator />
            
            <div class="row g-3">
                <div class="col-md-6">
                    <label class="form-label">Tên đăng nhập</label>
                    <input type="text" class="form-control" value="@Username" disabled />
                </div>

                <div class="col-md-6">
                    <label class="form-label">Mật khẩu hiện tại</label>
                    <InputText @bind-Value="ChangePasswordForm.CurrentPassword" class="form-control" type="password" />
                    <ValidationMessage For="() => ChangePasswordForm.CurrentPassword" class="text-danger" />
                </div>

                <div class="col-md-6">
                    <label class="form-label">Mật khẩu mới</label>
                    <InputText @bind-Value="ChangePasswordForm.NewPassword" class="form-control" type="password" />
                    <ValidationMessage For="() => ChangePasswordForm.NewPassword" class="text-danger" />
                </div>

                <div class="col-md-6">
                    <label class="form-label">Xác nhận mật khẩu</label>
                    <InputText @bind-Value="ChangePasswordForm.ConfirmPassword" class="form-control" type="password" />
                    <ValidationMessage For="() => ChangePasswordForm.ConfirmPassword" class="text-danger" />
                </div>
            </div>
        </EditForm>
    </BodyTemplate>
    <FooterTemplate>
        <Button Color="ButtonColor.Secondary" @onclick="Close">Hủy</Button>
        <Button Color="ButtonColor.Primary" @onclick="HandleSubmit" Disabled="@IsChangingPassword">
            @if (IsChangingPassword)
            {
                <Spinner Color="SpinnerColor.Light" Size="SpinnerSize.Small" class="me-2" />
            }
            Đổi mật khẩu
        </Button>
    </FooterTemplate>
</Modal>

@code {
    private Modal modal = default!;
    [Parameter]
    public UsersChangePasswordRequest ChangePasswordForm { get; set; } = new();

    [Parameter]
    public string Username { get; set; } = string.Empty;

    [Parameter]
    public bool IsChangingPassword { get; set; }

    [Parameter]
    public EventCallback OnClose { get; set; }

    [Parameter]
    public EventCallback OnSubmit { get; set; }

    public async Task ShowAsync()
    {
        await modal.ShowAsync();
    }

    public async Task HideAsync()
    {
        await modal.HideAsync();
    }

    private async Task HandleSubmit()
    {
        await OnSubmit.InvokeAsync();
    }

    private async Task Close()
    {
        await HideAsync();
        await OnClose.InvokeAsync();
    }
}
