<aside class="sidebar d-none d-md-block">
    <div class="sidebar-header">
        <h4 class="sidebar-brand">HGS Portal</h4>
    </div>
    <nav class="sidebar-nav">
        <NavLink class="sidebar-link" href="" Match="NavLinkMatch.All">
            <i class="bi bi-house-door-fill"></i>
            <span>Home</span>
        </NavLink>
        <NavLink class="sidebar-link" href="counter">
            <i class="bi bi-plus-square-fill"></i>
            <span>Counter</span>
        </NavLink>
        <NavLink class="sidebar-link" href="weather">
            <i class="bi bi-list-nested"></i>
            <span>Weather</span>
        </NavLink>
        <NavLink class="sidebar-link" href="login">
            <i class="bi bi-person"></i>
            <span>Login</span>
        </NavLink>
        <NavLink class="sidebar-link" href="users">
            <i class="bi bi-people"></i>
            <span>Users</span>
        </NavLink>
        <NavLink class="sidebar-link" href="roles">
            <i class="bi bi-shield"></i>
            <span>Roles</span>
        </NavLink>
        <NavLink class="sidebar-link" href="menus">
            <i class="bi bi-list-check"></i>
            <span>Menus</span>
        </NavLink>
        <NavLink class="sidebar-link" href="audit">
            <i class="bi bi-clock-history"></i>
            <span>Audit Logs</span>
        </NavLink>
        <NavLink class="sidebar-link" href="permission-delegation">
            <i class="bi bi-shield-check"></i>
            <span>Quản lý phân quyền</span>
        </NavLink>
        <NavLink class="sidebar-link" href="profile">
            <i class="bi bi-person-circle"></i>
            <span>Profile</span>
        </NavLink>
    </nav>
</aside>

<style>
    /* Desktop Sidebar */
    .sidebar {
        background: linear-gradient(180deg, var(--hgs-blue) 0%, var(--hgs-blue-dark) 100%);
        color: white;
        min-height: 100vh;
        position: sticky;
        top: 0;
        width: 260px;
        flex-shrink: 0;
        box-shadow: var(--shadow);
    }

    .sidebar-header {
        padding: 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .sidebar-brand {
        color: white;
        font-weight: 600;
        margin: 0;
        font-size: 1.25rem;
    }

    .sidebar-nav {
        padding: 1rem 0;
    }

    .sidebar-link {
        display: flex;
        align-items: center;
        padding: 0.75rem 1.5rem;
        color: #E8EEF8;
        text-decoration: none;
        transition: var(--transition);
        gap: 0.75rem;
    }

    .sidebar-link:hover {
        background: rgba(255, 255, 255, 0.12);
        color: white;
        padding-left: 1.75rem;
    }

    .sidebar-link.active {
        background: var(--hgs-orange);
        color: white;
        font-weight: 600;
        box-shadow: 0 2px 8px rgba(245, 130, 32, 0.35);
    }

    .sidebar-link i {
        width: 20px;
        height: 20px;
        font-size: 1.1rem;
    }
</style>

